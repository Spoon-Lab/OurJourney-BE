package pudding.toy.ourJourney.dto.thread;

import java.time.LocalDateTime;

public class GetThreadDto {
    public GetThreadDto(long l, String thread, ThreadProfileDto threadProfileDto, LocalDateTime now) {
    }
}
